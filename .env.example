# EmailJS — contact form delivery
# Create a service + template at https://www.emailjs.com and copy the IDs here.
# The public key is safe to expose (it's scoped by domain in the EmailJS dashboard).
NEXT_PUBLIC_EMAILJS_SERVICE_ID=
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=
